# report-coffee

CLI-скрипт для построения отчёта `median-coffee` по одному или нескольким CSV-файлам.
Отчёт считает медиану трат на кофе (`coffee_spent`) по каждому студенту за все переданные файлы и выводит таблицу в консоль, отсортированную по убыванию медианы.

## Пример запуска

```bash
python main.py --files math.csv physics.csv programming.csv --report median-coffee
```

или через bat-обёртку:

```bat
run_report.bat
```

## Bat-скрипты

- `run_report.bat` — обычная версия. Запускает `main.py` из этого репозитория и автоматически подставляет все `*.csv` из текущей папки.
- `run_report_portable.bat` — portable-версия. Её можно скопировать в любую папку с CSV-файлами и запустить там, даже без структуры проекта: расчёт медианы выполняется внутри PowerShell-скрипта.

## Полный пример вывода

```text
+-------------------+-----------------+
| student           |   median_coffee |
+===================+=================+
| Иван Кузнецов     |             700 |
+-------------------+-----------------+
| Дмитрий Морозов   |             610 |
+-------------------+-----------------+
| Михаил Павлов     |             590 |
+-------------------+-----------------+
| Никита Соловьев   |             560 |
+-------------------+-----------------+
| Алексей Смирнов   |             530 |
+-------------------+-----------------+
| Сергей Козлов     |             480 |
+-------------------+-----------------+
| Павел Новиков     |             450 |
+-------------------+-----------------+
| Артем Григорьев   |             420 |
+-------------------+-----------------+
| Елена Волкова     |             340 |
+-------------------+-----------------+
| Дарья Петрова     |             310 |
+-------------------+-----------------+
| Татьяна Васильева |             270 |
+-------------------+-----------------+
| Анна Белова       |             210 |
+-------------------+-----------------+
| Ольга Новикова    |             200 |
+-------------------+-----------------+
| Виктория Федорова |             150 |
+-------------------+-----------------+
| Мария Соколова    |             140 |
+-------------------+-----------------+
```

## Архитектура проекта

- `main.py` — точка входа, делегирует выполнение в `report_coffee.cli.main`.
- `report_coffee/cli.py` — обработка CLI-параметров `--files` и `--report`, обработка пользовательских ошибок, вывод таблицы в консоль.
- `report_coffee/csv_reader.py` — чтение и объединение строк из всех переданных CSV-файлов, базовые проверки входных данных.
- `report_coffee/report_registry.py` — реестр отчётов (`имя отчёта -> функция построения`), чтобы удобно добавлять новые отчёты без изменения CLI-логики.
- `report_coffee/reports/median_coffee.py` — бизнес-логика отчёта `median-coffee`: группировка по студенту, расчёт медианы, сортировка по убыванию.
- `tests/` — pytest-тесты для CLI-сценариев, обработки ошибок и проверки расчётов.

Почему так:

- разделены обязанности между слоями (CLI, чтение данных, отчёты);
- добавление нового отчёта сводится к новой функции в `reports/` и записи в `REPORTS` в реестре;
- такая структура упрощает тестирование и поддержку.

## Тесты

Запуск:

```bash
pytest -q
```

Что покрыто тестами:

- успешный сценарий построения отчёта через CLI;
- ошибка при неизвестном `--report`;
- ошибка при несуществующем файле;
- ошибка при невалидном значении `coffee_spent`;
- ошибка при отсутствии обязательной колонки `coffee_spent`;
- ошибка при пустом поле `student`;
- объединение данных из нескольких файлов через `--files`;
- корректный расчёт медианы и сортировка по убыванию в отчёте.

## Параметры

- `--files` — список путей к CSV-файлам.
- `--report` — имя отчёта (в этом задании: `median-coffee`).
